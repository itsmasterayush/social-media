# MongoDB Atlas Connection String
MONGODB_URI=mongodb+srv://itsraiayush:Papa20101977@cluster.mongodb.net/social_media_db?retryWrites=true&w=majority

# Secret key for JWT signing
JWT_SECRET=super_secret_jwt_key_change_in_production_32chars

# Base API URL (used by client-side Axios if needed, default relative /api)
NEXT_PUBLIC_API_URL=/api

# Node Environment
NODE_ENV=development
